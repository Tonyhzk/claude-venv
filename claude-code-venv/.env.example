# Claude Code 环境变量配置示例
# 此文件包含 API 密钥和代理设置等敏感信息
# 请勿提交到版本控制系统

# ==================== API 配置 ====================
# Anthropic API 认证令牌（必填）
# 从 https://console.anthropic.com/ 获取
ANTHROPIC_AUTH_TOKEN=your_api_key_here

# Anthropic API 基础 URL（可选）
# 如果使用自定义 API 端点，请取消注释并修改
ANTHROPIC_BASE_URL=https://api.anthropic.com

# ==================== cc switch 接管 API ====================
# 如果你使用 cc switch 来管理 API，只需修改以下两个环境变量：
# ANTHROPIC_AUTH_TOKEN=PROXY_MANAGED
# ANTHROPIC_BASE_URL=http://127.0.0.1:15721
#
# 这样 Claude Code 会通过本地代理来管理 API 调用

# ==================== 模型配置 ====================
# 默认使用的模型版本
# 可以根据需要修改为其他版本
ANTHROPIC_DEFAULT_HAIKU_MODEL=claude-haiku-4-5-20251001
ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4-5-20251101
ANTHROPIC_DEFAULT_SONNET_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_REASONING_MODEL=claude-sonnet-4-5-20250929

# ==================== 代理配置 ====================
# 如果需要使用代理，取消下面两行的注释并修改端口
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# 排除不走代理的域名（可选）
# NO_PROXY=localhost,127.0.0.1,.local

# ==================== Claude 配置 ====================
# Claude 用户目录（相对于脚本目录，或使用绝对路径）
# 默认：.claude（即 claude-code-venv/.claude）
CLAUDE_CONFIG_DIR=.claude

# ==================== 其他配置 ====================
# 禁用自动更新
DISABLE_AUTOUPDATER=1

# ==================== 使用说明 ====================
# 1. 复制此文件为 .env
#    cp .env.example .env
#
# 2. 编辑 .env 文件，填入你的 API 密钥
#    ANTHROPIC_AUTH_TOKEN=sk-ant-xxxxx
#
# 3. 如果需要使用代理，取消代理配置的注释并修改端口
#
# 4. 保存文件后，运行 python run.py 启动 Claude Code
#
# 注意：
# - .env 文件包含敏感信息，已添加到 .gitignore
# - 不要将 .env 文件提交到版本控制系统
# - 如果更新了配置，需要重启 Claude Code 才能生效
